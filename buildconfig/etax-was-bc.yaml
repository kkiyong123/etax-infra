apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: etax-was
  namespace: etax-app
  labels:
    app: etax-was
spec:
  source:
    type: Git
    git:
      uri: https://github.com/kkiyong123/etax-was
      ref: main
    contextDir: .
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: etax-was:latest
  triggers:
  - type: GitHub
    github:
      secret: etax-was-webhook-secret
  - type: ConfigChange
